'use strict';



describe ('Routes', () => {

  describe('Categories', () => {
    it('returns 200 http code on GET', (done) => {
      
    });

    it('returns a 200 and the new category on POST', done => {
      return request(server).post('/categories')
    
    })
  });
});